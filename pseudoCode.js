/* 
  create a function named showAll
  Pass one parameter (data)

  Inside the function
  Iterate through data
    for each object create element 
    create parent div 
      with the id/class '#container-' + object.id
    create a h1
      append/add inner.HTML to h1
    Repeat for every value
    append all elements + values to the parent div
*/

function showAll(data) {
  for (var key in data) {
    
  }
  }
}